import React from 'react';
import styles from './Footer.less';

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <h3>Alltid en ny batch på gång!</h3>
        </footer>
    );
};

export default Footer;